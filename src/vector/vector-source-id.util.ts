export const sourceIdForResource = (id: number): string => `resource_${id}`;
export const sourceIdForDoc = (id: number): string => `doc_${id}`;
export const sourceIdForNote = (id: number): string => `note_${id}`;
export const sourceIdForKnowledge = (id: number): string => `knowledge_${id}`;
